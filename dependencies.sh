#!/usr/bin/env bash
# not sure we care about which
# we can figure out which ones we are using
# Install with
#    pip install pipreqs
# Regenerate requirements.txt with
#     pipreqs --force
#
# pipreqs will generate the EXACT current version which is probably too fine
# Edit to the major or major.minor numbers
#
pip3 install -r requirements.txt
