# Install into the project virtualenv with `bash dependencies.sh`, which
# creates .venv if missing and installs into it. Do not pip install these
# against the system Python — the point of .venv is that this project is
# insulated from whatever Python the host machine happens to carry.
#
# Every version is pinned, direct and transitive, so a fresh checkout resolves
# to exactly the environment this was developed and tested against.

# --- direct dependencies ---
elasticsearch==9.4.1
numpy==2.4.6
pandas==3.0.5
pytest==9.1.1
requests==2.34.2
tqdm==4.70.0

# --- transitive dependencies (pinned for reproducibility) ---
anyio==4.14.2
certifi==2026.7.22
charset-normalizer==3.4.9
elastic-transport==9.4.2
idna==3.18
iniconfig==2.3.0
packaging==26.2
pluggy==1.6.0
Pygments==2.20.0
python-dateutil==2.9.0.post0
six==1.17.0
sniffio==1.3.1
typing_extensions==4.16.0
urllib3==2.7.0
